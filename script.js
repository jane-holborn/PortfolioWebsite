function toggleMenu() {
    document.querySelector('.hamburgerIcon').classList.toggle('active');  // Toggling on hamburger icon
    document.querySelector('nav').classList.toggle('active');  // Toggling on nav menu
}